version: "3.7"  
services:
  terraforming-mars:
    container_name: terraforming-mars
    build: ./
    restart: unless-stopped
    ports:
      - "8081:8081"
    security_opt:
      - no-new-privileges:true
    volumes:
      - ./db:/usr/src/app/db
volumes:
  tm-db:
